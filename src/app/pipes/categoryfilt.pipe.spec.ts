import { CategoryfiltPipe } from './categoryfilt.pipe';

describe('CategoryfiltPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryfiltPipe();
    expect(pipe).toBeTruthy();
  });
});
